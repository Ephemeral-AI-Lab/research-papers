schema_version: 1
id: paper-env-smoke
version: 1
plan:
  schema_version: 1
  name: paper-env-smoke
  configuration_base:
    id: standard-local
    version: 1
    scope: all
  seed: 20260712
  environment:
    image: ubuntu:24.04@sha256:52df9b1ee71626e0088f7d400d5c6b5f7bb916f8f0c82b474289a4ece6cf3faf
    client_cohort: direct_client
  protocol:
    order: randomized_blocks
    resource_interval_ms: 100
    trial_defaults:
      fast: {warmups: 0, measured_trials: 1}
      destructive: {warmups: 0, measured_trials: 1}
    timeout_ms:
      default: 120000
      squash_layerstack: 120000
  operations:
    - operation: exec_command
      configuration:
        enabled: true
        factors:
          concurrent_requests: {role: controlled, values: [1]}
          workspace_profile: {role: controlled, values: [small]}
          session_mode: {role: controlled, values: [explicit]}
          command_case: {role: controlled, values: [noop]}
    - operation: create_workspace
      configuration:
        enabled: true
        factors:
          workspace_count: {role: controlled, values: [1]}
          workspace_profile: {role: controlled, values: [small]}
          network_profile: {role: controlled, values: [shared]}
