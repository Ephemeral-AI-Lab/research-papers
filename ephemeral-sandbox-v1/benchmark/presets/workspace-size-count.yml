schema_version: 1
id: workspace-size-count
version: 1
plan:
  schema_version: 1
  name: workspace-size-count
  configuration_base:
    id: standard-local
    version: 1
    scope: workspace
  seed: 20260712
  environment:
    image: ubuntu:24.04
    client_cohort: direct_client
  protocol:
    order: randomized_blocks
    resource_interval_ms: 100
    trial_defaults:
      fast: {warmups: 2, measured_trials: 30}
      destructive: {warmups: 1, measured_trials: 10}
    timeout_ms:
      default: 120000
      squash_layerstack: 600000
  operations:
    - operation: create_workspace
      configuration:
        enabled: true
        factors:
          workspace_count: {role: varied, values: [1, 5, 20], control: 1}
          workspace_profile: {role: varied, values: [small, medium, large], control: small}
          network_profile: {role: controlled, values: [shared]}
